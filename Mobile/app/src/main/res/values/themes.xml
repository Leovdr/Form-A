<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.AplikasiFormA" parent="Base.Theme.AplikasiFormA">
        <!-- Transparent system bars for edge-to-edge -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Status bar transparan -->
        <item name="android:windowLightStatusBar">true</item> <!-- Icon status bar berwarna gelap -->

        <!-- Primary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
    </style>

    <!-- Define the base theme -->
    <style name="Base.Theme.AplikasiFormA" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here -->
    </style>

</resources>
